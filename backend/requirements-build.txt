# Build-only dependencies (not needed at runtime)
# Pinned: unpinned Cython>=3.0 let the ACR remote build agent drift to a 3.1.x
# release carrying a codegen regression ("generate_sequence_as_array_code ...
# expected str instance, NoneType found") that crashes cythonize on our kwargs
# call sites. 3.2.4 transpiles all 21 background modules cleanly (verified).
Cython==3.2.4
setuptools>=69.0
