# FastAPI Backend Dependencies
# ============================

# Web Framework
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
python-multipart>=0.0.6

# Configuration
python-dotenv>=1.2.0
PyYAML>=6.0.3

# Database
# Pinned: unpinned falkordb>=1.0.0 let deploy builds drift to a client version
# that strictly rejected OrderedDict params, silently breaking Layer-4 session
# persistence in prod ("Graph state persist failed: Expected dict, got
# collections.OrderedDict"). 1.6.2 is verified end-to-end in the compiled dist.
falkordb==1.6.2

# AI/LLM
google-genai>=1.59.0
openai>=1.30.0
anthropic>=0.30.0

# Data Validation
pydantic>=2.12.0

# HTTP Client
httpx>=0.28.0

# Authentication
python-jose[cryptography]>=3.3.0

# Excel / Spreadsheets
openpyxl>=3.1.5

# Utilities
requests>=2.31.0
tabulate>=0.9.0
